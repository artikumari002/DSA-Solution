class Solution {
    void pushZerosToEnd(int[] arr) {
        int brr[]=new int[arr.length];int c=0;
        for(int i=0;i<arr.length;i++)
        {
            if(arr[i]!=0)
            brr[c++]=arr[i];
        } 
        for(int i=0;i<arr.length;i++)
      arr[i]=brr[i];
    }
}

